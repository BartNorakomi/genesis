#ifndef ROOM_ARCADE1_H
#define ROOM_ARCADE1_H

// ---------------------------------------------------------
// 1. Includes (must come before anything that uses them)
// ---------------------------------------------------------
#include <genesis.h>
#include "game_state.h"     // for GameState
#include "rooms.h"          // for ROOM_ARCADE1 enum

// ---------------------------------------------------------
// 2. Public API
// ---------------------------------------------------------
GameState runArcade1(void);

#endif
